{% extends "::base.html.twig" %}


{% block body %}
    <h2 align="center"> List of all Your Phones :</h2>

    <br>
    {% block button_add_new2 %}
        <form align="center" action="{{ path('app_phone_new') }}">
            <input type="submit" value="Add new phone"></form>
        <br>
    {% endblock%}
    <br>
    {% for phone in phones %}
        <p align="center"> ______________________________ </p>
        <p align="center">
            {{ phone.phone }} {{ phone.description }}
        </p>

            <div>
                <span>
                    <form align="center" action="{{ path('app_phone_delete', {"id":phone.id}) }}" method="POST">
                        <input name="_method" type="hidden" value="DELETE">
                        <input style="background-color: #C82829" type="submit" value="Delete">
                     </form>
               </span>
               <span>
                    <form align="center" action="{{ path("app_phone_showone", {"id":phone.id}) }}">
                        <input style="background-color: #ffcc00" type="submit" value="Show phone details">
                    </form>
                </span>
        </div>

    {% endfor %}
    <p>
    <br>
        <form align="center" action="{{ path('app_general_welcome') }}">
            <input style="background-color: cadetblue; font-weight: bold" type="submit" value="Main Page">
        </form>
    </p>
{% endblock %}
